# .NET 10 runs tests through Microsoft.Testing.Platform. xUnit v3 hosts it directly, so the
# VSTest bridge is not involved and test projects are ordinary executables.
[dotnet.test:runner]
name = "Microsoft.Testing.Platform"
