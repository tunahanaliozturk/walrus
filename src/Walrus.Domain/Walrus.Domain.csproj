<Project Sdk="Microsoft.NET.Sdk">

  <!--
    No dependencies. Everything that decides what a sink ends up holding lives here as pure functions, so the
    convergence claims are tested as properties without a database in sight.
  -->

  <ItemGroup>
    <InternalsVisibleTo Include="Walrus.UnitTests" />
  </ItemGroup>

</Project>
